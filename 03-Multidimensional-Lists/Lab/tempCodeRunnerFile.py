            # current_sum = matrix[rows][cols] + matrix[rows][cols + 1] \
            #      + matrix[rows + 1][cols] + matrix[rows + 1][cols + 1]
            # if current_sum > biggest_sum:
            #     biggest_sum = current_sum